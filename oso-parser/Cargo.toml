[package]
name = "oso-parser"
version = "0.1.0"
edition = "2021"
description = "Ọ̀ṢỌ́ language tokenizer, parser, and IR — Rust port of OSOVM/src/oso_compiler.jl"

[dependencies]
serde       = { version = "1", features = ["derive"] }
serde_json  = "1"
thiserror   = "1"

[dev-dependencies]
# no extra deps needed
