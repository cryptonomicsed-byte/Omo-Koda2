[package]
name = "nautilus_integration"
version = "0.1.0"
edition = "2021"
description = "TEE private memory sealing via Nautilus (stub)"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
