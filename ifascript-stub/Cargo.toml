[package]
name        = "ifascript"
version     = "0.1.0"
edition     = "2021"
description = "Ifascript — vendored stub used when Bino-Elgua/Ifascript is not accessible"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
